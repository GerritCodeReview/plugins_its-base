[alias]
  all = //:all
  plugin = //:its-base
  its-base = //:its-base
  src = //:its-base-sources
  doc = //:its-base-javadoc
  mvn_install = //:install
  mvn_deploy = //:deploy

[java]
  src_roots = java, resources

[project]
  ignore = .git

[cache]
  mode = dir
  dir = buck-out/cache

